# 多副本：uvicorn 端口（默认 8100，各副本需不同）
OCR_PORT=8100
# 多副本：hybrid 子进程端口段必须错开（本副本占用 OCR_HYBRID_PORT..+OCR_HYBRID_WORKERS-1）
OCR_ENGINE=opendataloader
OCR_HYBRID_OCR_ENGINE=rapidocr
OCR_HYBRID_PORT=5002
OCR_HYBRID_WORKERS=2
OCR_HYBRID_DEVICE=cuda
OCR_MAX_SIDE=3000
