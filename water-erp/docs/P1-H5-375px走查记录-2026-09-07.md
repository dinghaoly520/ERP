# P1 遗留 H5：375px 手机宽度专项走查记录（2026-09-07）

> 口径来自《P1审查问题修复方案-2026-09-04.md》:266 验收项：要点卡折叠、解密表滚动、签署区块、台账面板。
> 走查在 H4 色板令牌化（`ad19cd1c`）之后进行——同轮顺带验证了 `--bid-stage-*` 令牌在 375px 下正常解析。
> 性质：**记录不修**（审查原裁定：目标用户为桌面办公场景，风险低）。

## 走查环境

- Chrome DevTools 设备仿真 375×812×2 mobile/touch（`resize_page` 会被 Chrome 最小窗宽钳到 500，须用 emulate）
- 账号：四川水发建设有限公司（英雄项目 cmqhero-bid-proj01·EVALUATING）、成都华西物资供应有限公司（contracts 空态）
- 截图：`/tmp/h5-375-*.png`（detail-top / receipt-card / sign-dialog / opening-hall）

## 结果总表

| # | 块 | 页面 | 375px 结果 | 判定 |
|---|---|---|---|---|
| 1 | 要点卡（A-87 招标文件要点） | `/bids/cmqhero-bid-proj01` | 卡片 298px 自适应、无溢出；本轮空态（「解析中或尚未生成」），三分组折叠结构无法实测 | ✅ 空态通过；有数据态未验（种子无已解析项目） |
| 2 | 签署区块（A-111 投标回执） | 同上 | 卡 298px ✓；「签署回执（U盾）」弹窗 `gdlg-pn max-w-full` 实测 311px 居中、viewport 无溢出 | ✅ 通过 |
| 3 | 解密表（供应商开标大厅） | `/my-bids/cmqhero-bid-proj01/opening-hall` | 唱标记录/开标记录双 `<table>` 700px 定宽、布局列 `.left` 740px 不折行（right 边缘 776px）；靠 `main.sp-content{overflow-x:auto}`（clientW 346 / scrollW 764）横向滚动可达，数据无丢失 | ⚠️ 功能可达、未做移动适配（P3，记录不修） |
| 4 | 台账面板（合同台账） | `/contracts` | 两家供应商均空态（合同模块晚于种子数据）；空态无溢出 ✓ | ✅ 空态通过；有数据行未验（盲区） |
| 附 | 详情/我的投标/中标通知 全局 | `/bids/[id]`、`/my-bids`、`/award-letters` | 页面级均无横向溢出（唯一越界元素为装饰性 `flow-glow` ±2px，无碍） | ✅ 通过 |

## 复核记录（避免误判）

- 首判「内容被裁不可达」有误：`document.scrollWidth=375` 是因 `BODY/.sp-layout/.sp-body overflow(-x)=hidden`；真正的滚动容器是 `main.sp-content`（overflow-x:auto，scrollW 764），横向可达。已修正裁定。
- 解密实操（OPENING 解密窗口）未跑——项目已 EVALUATING；大厅内同表（唱标记录）已渲染历史数据，等效验证了表格行为。

## 遗留建议（不在本批）

1. **解密表移动适配**（若未来供应商端开放手机参会）：`hall` 布局列 375px 断点折行 + 表格包 `overflow-x:auto` 容器（同 `sp-content` 手法）。
2. **台账/要点卡有数据态复验**：造一条合同/已解析要点数据后再补一轮（各 10 分钟）。
