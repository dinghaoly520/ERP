# 快照项目（JJ-2026091003）组长/组员双角色全流程审查报告

**审查日期**：2026-09-21
**审查载体**：快照 `scripts/snapshots/JJ-2026091003-demo-0914.json`（引大济岷工程千隧ZK10/千隧ZK12钻孔施工技术服务，竞价采购，3 家投标 + **AI 分析 COMPLETED** + 45 条评分 + 5 正选 5 候补）
**审查账号**：组员=李叶（技术组）、组长=彭明亮（技术组）；口令 `111111111111111111`
**审查方式**：快照两步恢复（restore-demo.sh + FileAsset 回灌）后，Chrome DevTools MCP 逐按钮实测 + 数据库落库验证
**代码基线**：main @ dd94e7c4（审查中发现并修复 2 个代码缺陷 + 1 个快照流程缺口）

---

## 一、结论速览

- **组长/组员双角色主链路全部可用**：登录→联系方式→7 步向导→（组员）等待末签提示→（组长）末签成功→动议/异议全链→平板端。
- **AI 辅助评标正常态首次完整验证**（快照含 AI 分析数据）：合规门/证据层/AI 总分/跨供应商对比/条款响应核对全部真实数据渲染，AI 分数与快照档案一致（99.6/99.1/99.3）。
- 审查中**发现并修复 2 个代码缺陷**（P1 候补口径污染聚合、P2 checklist 锁定态可篡改）与 **1 个快照恢复流程缺口**（解密产物 FileAsset 未回灌）。
- 测试产物已清理（异议/动议/澄清各 1 条）；快照停止点已按演示路径推进（组长末签 leaderCoSigned=t），restore-demo.sh 可随时复位。

## 二、审查中发现的问题（含修复）

| # | 级别 | 问题 | 根因 | 修复 |
|---|------|------|------|------|
| 1 | **P1** | 快照项目实时板显示 5/10 签到、50% 进度；组员报告页无「等待组长末签」提示；**组长末签按钮永不渲染**（allMembersConfirmed 永假） | `expertPresence.totalExperts` 把候补专家算入（getProject L402 与 broadcastAggregatePresence 均无 `expertRole: '正选'` 过滤）——5 候补 reportConfirmed=false 使 `reportConfirmedCount === totalExperts` 恒假；候补未递补前非评标委员会成员（招标投标法第 37 条） | 两处查询加 `expertRole: '正选'`；验证：presence 5/5/100%、组员提示与组长末签按钮恢复 |
| 2 | **P2** | 评分已锁定（报告已确认）状态下，checklist 得分点 checkbox/数值输入**仍可点击篡改**（本地 state 变化，虽服务端拒交） | 桌面端两处 `PointChecklistScoring` 调用未传 `readOnly`（组件本身支持；平板端早已传） | 两处调用加 `readOnly={scoreLocked \|\| !canScoreActiveSupplier}`；验证：19 个 checkbox 全 disabled、点击无效 |
| 3 | 快照流程缺口 | 快照恢复后 3 家供应商标书「待解密」、无预览链接（FileAsset 74→80 行缺口） | 解密产物 FileAsset（bid_decrypted ×3）不在 keep 备份 6 行回灌清单内（历史恢复流程只补了招标文件/开标包/密文标书/AI 报告） | 从 keep 备份追加抽取 3 行回灌；恢复后预览/大小/已解密徽标全部正常 |

**附带环境修复**：快照恢复首跑 P2022（ProjectManagementItem.terminationReason）——根因=本库未应用 `20260920000000_project_termination` / `20260920010000_project_termination_audit` 两个迁移（schema 已含列、client 已生成、DB 缺列）；`prisma migrate deploy` + `generate` 后恢复通过。demo-snapshot.js REMOVED_FIELDS 增补 terminationReason（防御旧快照）。

**快照时间老化处理**：快照 evaluationDeadline=09-13（捕获时值，已过期）→ 审查中延至 now+48h（演示态 SQL，非代码缺陷）。

## 三、组员（李叶）逐按钮测试记录

| 区域 | 按钮/交互 | 结果 |
|------|-----------|------|
| 登录页 | 专家登录 tab / 账号密码 / 登录 | ✅ |
| 仪表盘 | 联系方式弹窗（手机号预填/校验/确认） | ✅ |
| 向导框架 | 步骤指示器 7 步、智能跳步至报告步、截止倒计时横幅 | ✅ |
| Step1 身份核验 | 完成态四段（签到/保密/纪律/AI 声明）+ 回避区块 + 核验完成条 | ✅ |
| Step2 标书获取 | 招标文件预览（PDF）、澄清修改文件卡、3 家已解密标书预览、刷新按钮 | ✅（修复 #3 后） |
| Step3 辅助评标 | **AI 正常态**：合规门（6/0/0/2 条款）、证据层（OCR 结构化数据：法人/资质/报价/工期）、AI 总分 99.6→切华建 99.1、跨供应商对比、免责声明、供应商切换 | ✅（AI 分数与快照档案一致） |
| Step4 条款响应核对 | 52 条条款清单、资格要求 8 条、「查看」展开、异议/存疑标注、右栏评分项状态、「去打分平板」发送+ACK 轮询（无平板→正确提示） | ✅ |
| Step5 专家打分 | checklist 得分点（资格 8/8、符合 10/10、价格 1/1）、锁定横幅、提交按钮 disabled「评分已锁定」、价格项公式计算只读卡、评分历史抽屉（下拉切换：华建 100/四大队 99.4） | ✅（修复 #2 后 checkbox 全 disabled） |
| Step6 核对 | 3 家切换、「无需再核对」锁定态、评分明细（公式理由）、备注抽屉（手写/键盘/保存文本/空态） | ✅ |
| Step7 评审报告 | 已确认徽标、排名三分数、无末签按钮、**「所有成员已确认，等待组长末签」提示**（修复 #1 后）、esign 等待签字包 | ✅ |
| 通用 | 开标消息弹窗（空态/只读公聊/Esc 关闭）、澄清答疑（历史空态/供应商选择/发送/已删 AI 起草按钮）、Esc 关闭 | ✅ |

## 四、组长（彭明亮）逐按钮测试记录

| 区域 | 按钮/交互 | 结果 |
|------|-----------|------|
| 登录/联系方式 | 同上 | ✅ |
| Step7 评审报告 | 「所有成员已确认，请组长末签」提示 + **组长末签按钮** → confirm → 末签成功（leaderCoSigned=t 落库）→ 刷新后按钮消失、「组长已末签，可生成评标结果」提示 | ✅ 全链真 API |
| tasks 评审待办 | 「发起动议」按钮+表单开合 → 真实发起（「表决已发起」）→ 本人投票（「投票成功」+「已投：赞成」）→ 「结束投票·形成决议」→ **法定人数闸门**（1/5 不足 3 人 → 正确拒绝） | ✅ |
| tasks 异议工单 | 「提交异议」按钮+表单 → 真实提交（「异议已提交」+「待裁决」状态） | ✅ |
| projects | 项目卡（引大济岷）+ 评审中/已归档/全部筛选 | ✅ |
| profile | 个人资料/统计/编辑资料/查看全部按钮 | ✅ |
| 平板端 | 快照项目加载、锁定态、checklist、备注双模式、总分 | ✅ |

## 五、未覆盖项

- 候补专家视角（5 候补未签到——候补在递补前不可评标，服务端 SUBSTITUTE_EXPERT 闸在册，未浏览器走查）
- 真实摄像头拍照（无头环境，同前次审查）
- 签字包生成后的 esign 真签（快照停止点在末签前；末签后生成评标结果/签字包是 :3007 演示链，本次未推进）
- 评标超时锁定（deadline 已延至未来，超时 409 场景未触发）

## 六、数据变更与复位

- 快照停止点推进：leaderCoSigned=t（演示路径正常推进；restore-demo.sh 一键复位）
- 测试产物已清理：动议/异议/澄清各 1 条（SQL 删除）
- FileAsset 从 74→80 行（keep 回灌 6 + 解密产物 3——**建议**：将解密产物 3 行纳入 restore 文档/脚本回灌清单，否则下次恢复仍缺标书预览）
- evaluationDeadline 延至 now+48h（演示态）
- 2 个代码修复（#1/#2）+ demo-snapshot.js 消毒映射补强待提交
