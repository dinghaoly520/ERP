# 异议/疑问备注引用评分理由 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把专家打分页现有的「📎 插入异议」手动两段式机制，改造合并为"聚焦理由框自动展开 + 复选框勾选追加"的单一面板（保留📎按钮作手动开关），并把后端覆盖范围从 dispute 扩到 dispute+doubt。

**Architecture:** 后端抽公共方法 `buildExpertReviews(projectId, expertId, verdicts)`，`getMyScores`（dispute+doubt）与 `getReport.myDisputedReviews`（dispute）共用；`disputesBySupplier` 每 item 增 `verdict`。前端在 `page.tsx` 内联 `ReviewNotePanel`，聚焦/按钮共控单一 `reviewPanelOpenKey`，复选框走 `insertedKeys` 幂等追加。

**Tech Stack:** NestJS 11 + Prisma（后端，Jest 单测）；Next.js 16 + React 19（前端，无组件测试基建，靠 tsc + 手测）。

## Global Constraints

- 当前在 `main` 分支：实现前先开 `feat/review-note-panel` 分支。
- 后端所有改动保持"无 `DEEPSEEK_API_KEY` / 失败降级"约定（本特性不调 LLM，不涉及）。
- 守"专家独立落字"：备注仅可勾选素材，绝不自动落分或裁定通过/不通过。
- 软门（`disputeCategoriesBySupplier` + `confirmedDispute`）行为不变，仍 dispute-only。
- 后端单测命令：`pnpm --filter api test -- expert.service.spec`；前端类型检查：`cd apps/expert-portal && npx tsc --noEmit`。
- 提交信息用仓库惯例（`fix(expert):` / `feat(expert-portal):`），中文描述。

## File Structure

- `apps/api/src/expert/expert.service.ts` — 改 `getMyScores`（disputesBySupplier 扩 doubt + item 加 verdict）；新增 private `buildExpertReviews`；改 `getReport` 复用之。
- `apps/api/src/expert/expert.service.spec.ts` — 改 `getMyScores` 现有断言（加 verdict/doubt）；加 `getReport.myDisputedReviews` 行为不变测试。
- `apps/expert-portal/src/app/(app)/evaluate/[id]/page.tsx` — 改造「📎 插入异议」区块为内联 `ReviewNotePanel`（pass-fail 与数值两处）；新增 state/helper；删旧「插入」按钮与 `disputeInsertOpenKey`。
- `apps/expert-portal/src/lib/types.ts` — `disputesBySupplier` item 类型加 `verdict`。

---

## Task 1: 后端 — disputesBySupplier 扩 doubt + 抽 buildExpertReviews + getReport 复用

**Files:**
- Modify: `apps/api/src/expert/expert.service.ts`（`getMyScores` :819-886；`getReport.myDisputedReviews` :~903-932；新增 private 方法）
- Test: `apps/api/src/expert/expert.service.spec.ts`（`getMyScores` describe :658-733）

**Interfaces:**
- Produces: `ExpertService.buildExpertReviews(projectId: string, expertId: string, verdicts: ('dispute'|'doubt')[]) => Promise<Array<{ category, verdict, requirementId, note, supplierId, supplierName, content }>>`；`getMyScores` 返回的 `disputesBySupplier` item 形状变为 `{ requirementId, content, note, verdict }`。

- [ ] **Step 1: 开分支**

```bash
cd /home/asus/桌面/ERP && git checkout -b feat/review-note-panel
```

- [ ] **Step 2: 改写 getMyScores 的 doubts 测试（加 doubt + verdict 断言）**

把 `expert.service.spec.ts` 里 `it('增返 disputesBySupplier ...')`（约 :684-719）整段替换为：

```ts
    it('disputesBySupplier 含 dispute + doubt，item 带 verdict；disputeCategoriesBySupplier 仍 dispute-only', async () => {
      prisma.bidExpert.findFirst.mockResolvedValue({ id: 'exp-1', userId: 'u1', signedIn: true, avoidanceConfirmed: true });
      prisma.bidScoreRecord.findMany.mockResolvedValue([]);
      prisma.bidRequirementReview = { findMany: jest.fn().mockResolvedValue([
        // s1 - technical dispute
        { category: 'technical', verdict: 'dispute', bidderResultId: 'br-1', requirementId: 'req-r1', note: 'n1', bidderResult: { bidSupplier: { id: 's1', supplierName: '供1' } } },
        // s1 - commercial doubt（应进 disputesBySupplier，不进 disputeCategoriesBySupplier）
        { category: 'commercial', verdict: 'doubt', bidderResultId: 'br-1', requirementId: 'req-r2', note: 'd2', bidderResult: { bidSupplier: { id: 's1', supplierName: '供1' } } },
        // s2 - technical dispute
        { category: 'technical', verdict: 'dispute', bidderResultId: 'br-2', requirementId: 'req-r3', note: 'n3', bidderResult: { bidSupplier: { id: 's2', supplierName: '供2' } } },
        // ack 不计（即使 mock 漏进也应被 JS 防御过滤）
        { category: 'qualification', verdict: 'ack', bidderResultId: 'br-3', requirementId: 'req-r4', note: 'x', bidderResult: { bidSupplier: { id: 's3', supplierName: '供3' } } },
      ]) };
      prisma.aiBidderResult.findMany = jest.fn().mockResolvedValue([
        { id: 'br-1', requirementResponses: [{ requirementId: 'req-r1', tenderContent: '内容1' }] },
        { id: 'br-2', requirementResponses: [{ requirementId: 'req-r3', tenderContent: '内容3' }] },
      ]);
      const out = await service.getMyScores('u1', 'proj-1');
      expect(out.disputesBySupplier).toEqual({
        s1: {
          TECHNICAL: [{ requirementId: 'req-r1', content: '内容1', note: 'n1', verdict: 'dispute' }],
          BUSINESS: [{ requirementId: 'req-r2', content: '', note: 'd2', verdict: 'doubt' }],
        },
        s2: {
          TECHNICAL: [{ requirementId: 'req-r3', content: '内容3', note: 'n3', verdict: 'dispute' }],
        },
      });
      // disputeCategoriesBySupplier 仍 dispute-only：s1 只 TECHNICAL（doubt 的 BUSINESS 不进）
      expect(out.disputeCategoriesBySupplier).toEqual({ s1: ['TECHNICAL'], s2: ['TECHNICAL'] });
    });
```

同时把同 describe 里 `it('返回 records + disputeCategoriesBySupplier ...')`（约 :659-682）的 mock 补一行 `prisma.aiBidderResult.findMany = jest.fn().mockResolvedValue([]);`（放在 `prisma.bidRequirementReview = ...` 之后），避免新公共方法调用未 mock 的 findMany 报错。

- [ ] **Step 3: 跑测试，确认失败**

```bash
pnpm --filter api test -- expert.service.spec
```
Expected: `getMyScores` 那条 FAIL（当前 item 无 verdict、不含 doubt）。

- [ ] **Step 4: 新增 private `buildExpertReviews` 方法**

在 `expert.service.ts` 的 `getMyScores` 方法**之后**插入：

```ts
  /** 汇总本人条款核对（dispute/doubt）为扁平 review 列表，供 getMyScores 与 getReport 复用。
   *  反查 AiBidderResult.requirementResponses 取 tenderContent；丢弃无 supplier 关联的孤儿记录。 */
  private async buildExpertReviews(
    projectId: string,
    expertId: string,
    verdicts: ('dispute' | 'doubt')[],
  ) {
    const rows = await this.prisma.bidRequirementReview.findMany({
      where: { projectId, expertId, verdict: { in: verdicts } },
      select: {
        category: true, verdict: true, bidderResultId: true, requirementId: true, note: true,
        bidderResult: { select: { bidSupplier: { select: { id: true, supplierName: true } } } },
      },
    });
    const brIds = [...new Set(rows.map((r) => r.bidderResultId))];
    const brs = brIds.length
      ? await this.prisma.aiBidderResult.findMany({ where: { id: { in: brIds } } })
      : [];
    const contentMap = new Map<string, string>();
    for (const br of brs) {
      for (const r of ((br.requirementResponses as any[]) ?? [])) {
        contentMap.set(`${br.id}:${r.requirementId}`, r.tenderContent ?? '');
      }
    }
    return rows
      .map((d) => ({
        category: d.category,
        verdict: d.verdict,
        requirementId: d.requirementId,
        note: d.note ?? '',
        supplierId: d.bidderResult?.bidSupplier?.id ?? '',
        supplierName: d.bidderResult?.bidSupplier?.supplierName ?? '',
        content: contentMap.get(`${d.bidderResultId}:${d.requirementId}`) ?? '',
      }))
      // 防御：信任 query verdict 过滤，但兼容 mock/历史 Json 漂移
      .filter((r) => r.supplierId && (r.verdict === 'dispute' || r.verdict === 'doubt'));
  }
```

- [ ] **Step 5: 重写 `getMyScores` 用 buildExpertReviews**

把 `getMyScores`（:819-886）整段替换为：

```ts
  async getMyScores(userId: string, projectId: string) {
    const expert = await this.prisma.bidExpert.findFirst({
      where: { userId, projectId },
    });
    if (!expert) throw new ForbiddenException({ error: '您不是该项目的评审专家', code: 'NOT_PROJECT_EXPERT' });
    if (!expert.signedIn || !expert.avoidanceConfirmed) {
      throw new ForbiddenException({ error: '请先完成身份核验和回避确认', code: 'VERIFICATION_REQUIRED' });
    }

    const [records, reviews] = await Promise.all([
      this.prisma.bidScoreRecord.findMany({
        where: { expertId: expert.id },
        include: { scoreItem: true },
      }),
      this.buildExpertReviews(projectId, expert.id, ['dispute', 'doubt']),
    ]);

    const UPPER: Record<string, string> = {
      qualification: 'QUALIFICATION',
      technical: 'TECHNICAL',
      commercial: 'BUSINESS',
    };
    // disputeCategoriesBySupplier 仍 dispute-only（软门用）；disputesBySupplier 含 dispute+doubt
    const disputeCategoriesBySupplier: Record<string, string[]> = {};
    const disputesBySupplier: Record<string, Record<string, { requirementId: string; content: string; note: string; verdict: string }[]>> = {};
    for (const r of reviews) {
      const cat = UPPER[r.category];
      if (!cat) continue;
      if (r.verdict === 'dispute') {
        const catList = disputeCategoriesBySupplier[r.supplierId] ?? (disputeCategoriesBySupplier[r.supplierId] = []);
        if (!catList.includes(cat)) catList.push(cat);
      }
      const detailMap = disputesBySupplier[r.supplierId] ?? (disputesBySupplier[r.supplierId] = {});
      const detailList = detailMap[cat] ?? (detailMap[cat] = []);
      detailList.push({ requirementId: r.requirementId, content: r.content, note: r.note, verdict: r.verdict });
    }
    return { records, disputeCategoriesBySupplier, disputesBySupplier };
  }
```

- [ ] **Step 6: 重写 `getReport.myDisputedReviews` 复用 buildExpertReviews**

在 `getReport`（:931+）里，找到 `const disputed = await this.prisma.bidRequirementReview.findMany(...)` 到 `const myDisputedReviews = disputed.map(...)` 整段（约 :903-932，含中间的 brs/contentMap/brById），替换为：

```ts
    const disputedReviews = await this.buildExpertReviews(projectId, expert.id, ['dispute']);
    const myDisputedReviews = disputedReviews.map((r) => ({
      supplierId: r.supplierId,
      supplierName: r.supplierName,
      requirementId: r.requirementId,
      category: r.category,
      tenderContent: r.content,
      note: r.note,
    }));
```

（其后 `return { ... myDisputedReviews }` 不变。）

- [ ] **Step 7: 跑测试，确认通过**

```bash
pnpm --filter api test -- expert.service.spec
```
Expected: PASS（含改造后的 getMyScores 用例 + 原 orphan 用例 + disputeCategories 用例）。

- [ ] **Step 8: 加 getReport.myDisputedReviews 行为不变测试**

在 `expert.service.spec.ts` 末尾（或 getReport 的 describe 内，若没有就新增一个 `describe('getReport', ...)`）加：

```ts
  describe('getReport — myDisputedReviews', () => {
    it('改用 buildExpertReviews 后仍仅 dispute，含 supplierName 与 tenderContent 反查', async () => {
      prisma.bidExpert.findFirst.mockResolvedValue({ id: 'exp-1', userId: 'u1', projectId: 'proj-1', expertName: '专家', signedIn: true, avoidanceConfirmed: true, progress: 100 });
      prisma.user.findUnique.mockResolvedValue({ id: 'u1', username: 'u1', displayName: '专家' });
      prisma.bidProject.findUnique.mockResolvedValue({ id: 'proj-1', name: '项目', projectCode: 'P1', stage: 'EVALUATING', suppliers: [], scoreItems: [] });
      prisma.bidScoreRecord.findMany.mockResolvedValue([]);
      prisma.bidRequirementReview = { findMany: jest.fn().mockResolvedValue([
        { category: 'technical', verdict: 'dispute', bidderResultId: 'br-1', requirementId: 'r1', note: 'n1', bidderResult: { bidSupplier: { id: 's1', supplierName: '供1' } } },
        { category: 'technical', verdict: 'doubt', bidderResultId: 'br-1', requirementId: 'r2', note: 'n2', bidderResult: { bidSupplier: { id: 's1', supplierName: '供1' } } }, // doubt 不应出现
      ]) };
      prisma.aiBidderResult.findMany = jest.fn().mockResolvedValue([
        { id: 'br-1', requirementResponses: [{ requirementId: 'r1', tenderContent: '条款原文1' }] },
      ]);
      const out = await service.getReport('u1', 'proj-1');
      expect(out.myDisputedReviews).toEqual([
        { supplierId: 's1', supplierName: '供1', requirementId: 'r1', category: 'technical', tenderContent: '条款原文1', note: 'n1' },
      ]);
    });
  });
```

> 注：若 `getReport` 现有 mock 链与此不符，按现有 `getReport` 测试（如有）的 mock 基建对齐 `prisma.*` 字段；关键是断言 `myDisputedReviews` 仅含 dispute、含 supplierName 与 tenderContent。

- [ ] **Step 9: 跑全套 expert 单测，确认通过**

```bash
pnpm --filter api test -- expert.service.spec
```
Expected: PASS（含新 getReport 用例）。

- [ ] **Step 10: 提交**

```bash
cd /home/asus/桌面/ERP && git add apps/api/src/expert/expert.service.ts apps/api/src/expert/expert.service.spec.ts
git commit -m "feat(expert): disputesBySupplier 扩 doubt + 抽 buildExpertReviews 供 getReport 复用"
```

---

## Task 2: 前端 — 改造「📎 插入异议」为聚焦复选框面板

**Files:**
- Modify: `apps/expert-portal/src/app/(app)/evaluate/[id]/page.tsx`（state :150-152；pass-fail 区块 :1168-1203；数值区块 :1238-1268）
- Modify: `apps/expert-portal/src/lib/types.ts`（`disputesBySupplier` item 类型）

**Interfaces:**
- Consumes: Task 1 产出的 `disputesBySupplier[supplierId][CATEGORY] = [{ requirementId, content, note, verdict }]`。
- Produces: 聚焦理由框（或点📎按钮）展开复选框面板；勾选 → 追加 `【★条款 {requirementId}：{content 截断 40}】{note 或 content 兜底}` 到 `scores[k].reason`，幂等。

> 前端无组件测试基建：以 `tsc --noEmit` + 下方手测脚本为验证。

- [ ] **Step 1: 类型加 verdict**

`apps/expert-portal/src/lib/types.ts` 找到 `disputesBySupplier` 相关类型；若文件里没有显式类型（page.tsx 里内联了），则改 page.tsx 里的内联类型。在 page.tsx :152 把 state 类型改为：

```ts
  const [disputesBySupplier, setDisputesBySupplier] = useState<Record<string, Record<string, Array<{ requirementId: string; content: string; note: string; verdict: 'dispute' | 'doubt' }>>>>({});
```

同步改 :189 的 fetch 类型断言：

```ts
          disputesBySupplier: Record<string, Record<string, Array<{ requirementId: string; content: string; note: string; verdict: 'dispute' | 'doubt' }>>>;
```

- [ ] **Step 2: 换 state（删 disputeInsertOpenKey，加 reviewPanelOpenKey / insertedKeys / suppressBlurRef）**

page.tsx :150-152 附近，把 `const [disputeInsertOpenKey, setDisputeInsertOpenKey] = useState...`（若存在）删除，改为：

```ts
  const [reviewPanelOpenKey, setReviewPanelOpenKey] = useState<string | null>(null);
  const [insertedKeys, setInsertedKeys] = useState<Set<string>>(new Set());
  const suppressBlurRef = useRef(false);
```

> 若文件里没有 `useRef` import，在顶部 `import { ..., useRef }` 补上（已有则跳过）。

- [ ] **Step 3: 加 helper（noteId / buildInsertText / onToggleNote / 焦点处理器）**

在 `scoreKey` helper（:154）附近加：

```ts
  const noteId = (supplierId: string, requirementId: string) => `${supplierId}:${requirementId}`;

  const buildInsertText = (dsp: { requirementId: string; content: string; note: string }) => {
    const clause = dsp.content?.trim() ? dsp.content.slice(0, 40) : '(原文缺失)';
    const body = dsp.note?.trim()
      ? dsp.note
      : dsp.content?.trim() ? dsp.content : '(原文缺失)';
    return `【★条款 ${dsp.requirementId}：${clause}】${body}`;
  };

  const onToggleNote = (
    k: string,
    supplierId: string,
    dsp: { requirementId: string; content: string; note: string },
    itemId: string,
  ) => {
    const id = noteId(supplierId, dsp.requirementId);
    if (insertedKeys.has(id)) return; // 幂等：已插入不再插；取消勾选不动文字
    const text = buildInsertText(dsp);
    setScores(prev => {
      const cur = prev[k];
      const prevReason = cur?.reason ?? '';
      const newReason = prevReason
        ? (prevReason.endsWith('\n') ? prevReason + text : prevReason + '\n' + text)
        : text;
      return { ...prev, [k]: { score: cur?.score ?? 0, reason: newReason, passed: cur?.passed } };
    });
    setInsertedKeys(prev => { const n = new Set(prev); n.add(id); return n; });
    if (missingReasons.has(itemId)) setMissingReasons(prev => { const n = new Set(prev); n.delete(itemId); return n; });
  };

  const onReasonFocus = (k: string) => setReviewPanelOpenKey(k);
  const onReasonBlur = () => {
    setTimeout(() => {
      if (!suppressBlurRef.current) setReviewPanelOpenKey(null);
      suppressBlurRef.current = false;
    }, 0);
  };
```

- [ ] **Step 4: 抽内联 ReviewNotePanel 渲染片段**

在组件内（`handleSubmitScores` 之后、return 之前）定义一个渲染函数：

```ts
  const renderReviewPanel = (
    k: string,
    category: string,
    itemId: string,
  ) => {
    const notes = disputesBySupplier[activeSupplier]?.[category] ?? [];
    if (notes.length === 0 || scoreLocked) return null;
    const open = reviewPanelOpenKey === k;
    return (
      <div className="mt-1.5">
        <button type="button"
          onClick={() => setReviewPanelOpenKey(prev => (prev === k ? null : k))}
          className="inline-flex items-center gap-1 text-[11px] font-semibold text-amber-700 hover:text-amber-800 hover:bg-amber-50 px-2 py-1 rounded-md transition">
          <AlertTriangle size={11} strokeWidth={1.5} /> {open ? '收起' : '插入异议/疑问'}
        </button>
        {open && (
          <div className="mt-1.5 rounded-lg border border-amber-200 bg-amber-50/80 p-2 space-y-1.5">
            {notes.map((dsp, idx) => {
              const id = noteId(activeSupplier, dsp.requirementId);
              const inserted = insertedKeys.has(id);
              return (
                <label key={`rev-${dsp.requirementId}-${idx}`}
                  onMouseDown={() => { suppressBlurRef.current = true; }}
                  className={`flex items-start gap-2 text-xs rounded px-2 py-1 ${inserted ? 'opacity-60' : 'hover:bg-white/70'}`}>
                  <input type="checkbox" checked={inserted} disabled={inserted}
                    onChange={() => onToggleNote(k, activeSupplier, dsp, itemId)}
                    className="mt-0.5 accent-amber-600" />
                  <div className="flex-1 min-w-0">
                    <span className={`text-[10px] font-bold px-1.5 py-0.5 rounded mr-1 ${dsp.verdict === 'dispute' ? 'bg-red-100 text-red-700' : 'bg-amber-100 text-amber-700'}`}>
                      {dsp.verdict === 'dispute' ? '异议' : '疑问'}
                    </span>
                    <span className="font-semibold text-amber-900">{dsp.content?.slice(0, 40) || '(原文缺失)'}</span>
                    {dsp.note && <div className="text-amber-700 mt-0.5">{dsp.note}</div>}
                    {inserted && <div className="text-[10px] text-amber-600 mt-0.5">已插入</div>}
                  </div>
                </label>
              );
            })}
          </div>
        )}
      </div>
    );
  };
```

- [ ] **Step 5: pass-fail 分支——挂焦点 + 替换旧块**

在 pass-fail 分支（:1158-1203）：

(a) 给「不通过理由」textarea 加 `onFocus`/`onBlur`（:1159 textarea 标签内）：

```tsx
                                    {verdict === false && (
                                      <textarea placeholder="不通过理由（必填）" value={val?.reason || ''}
                                        onFocus={() => onReasonFocus(k)}
                                        onBlur={onReasonBlur}
                                        onChange={e => { /* 原逻辑不变 */ }}
                                        className={/* 原样 */}
                                        aria-label={`${item.name} 不通过理由`} />
                                    )}
```

(b) 删掉旧的「📎 插入异议」整块（:1168-1203，从 `{/* Task 4: 📎 插入异议 ... */}` 到其闭合 `)}`），替换为：

```tsx
                                    {verdict === false && renderReviewPanel(k, category, item.id)}
```

- [ ] **Step 6: 数值分支——挂焦点 + 替换旧块**

在数值分支（:1230-1268）：

(a) 给「评分理由」textarea 加 `onFocus={() => onReasonFocus(k)} onBlur={onReasonBlur}`（:1230 textarea 标签内，与现有 onChange 并列）。

(b) 删掉旧的「📎 插入异议」整块（:1238-1268），替换为：

```tsx
                                  {renderReviewPanel(k, category, item.id)}
```

- [ ] **Step 7: 切供应商时关面板**

找到 `onSelect={(id) => { setActiveSupplier(id); setMissingReasons(new Set()); setConfirmedDispute({}); }}`（SupplierSidebar onSelect，约 :605），补 `setReviewPanelOpenKey(null);`：

```tsx
            onSelect={(id) => { setActiveSupplier(id); setMissingReasons(new Set()); setConfirmedDispute({}); setReviewPanelOpenKey(null); }}
```

- [ ] **Step 8: 类型检查**

```bash
cd /home/asus/桌面/ERP/water-erp/apps/expert-portal && npx tsc --noEmit
```
Expected: 无错误（退出码 0）。常见坑：`useRef` 未 import、`disputeInsertOpenKey` 残留引用——逐一清掉。

- [ ] **Step 9: 手测脚本**

启动 `pnpm --filter expert-portal dev`（或 `pnpm dev:expert`），用专家账号进入有 dispute/doubt 标注的项目评分页，逐项验证：

1. 聚焦某数值项理由框 → 面板自动展开，列出 dispute+doubt（带异议/疑问徽标）
2. 勾选一条 → 理由框追加 `【★条款 ...：...】...`；复选框变灰「已插入」
3. 再勾同一条 → 无重复追加（幂等）
4. 点「📎 插入异议/疑问」按钮 → 也能开关同一面板；点「收起」关闭
5. 切供应商 → 面板关闭、内容切换；切回原供应商 → 「已插入」标记保留
6. 只有 doubt（无 dispute）的类别 → 面板显示，但软门（类别顶部「⚠ 有异议条款待核对」+ confirmedDispute）**不触发**
7. pass-fail 选「不通过」→ 理由框聚焦后面板展开；选「通过」→ 无面板
8. RESPONSIVE / PRICE 类别 → 无面板
9. 确认评审报告（progress=100 后）→ 理由框只读、面板不渲染

- [ ] **Step 10: 提交**

```bash
cd /home/asus/桌面/ERP && git add apps/expert-portal/src/app/\(app\)/evaluate/\[id\]/page.tsx apps/expert-portal/src/lib/types.ts
git commit -m "feat(expert-portal): 评分理由引用异议/疑问改聚焦复选框面板（合并📎按钮）"
```

---

## Self-Review（写计划后自查）

- **Spec coverage**：spec §4（后端扩 doubt + verdict + buildExpertReviews）→ Task 1；§5（前端改造面板）→ Task 2；§6 边界（切供应商、pass-fail、doubt-only、锁定）→ Task 2 手测 5-9；§7 四个文件 → 都有对应 step。✓
- **Placeholder**：所有 step 含完整代码/命令；无 TBD。✓
- **Type 一致**：`buildExpertReviews` 返回字段名（category/verdict/requirementId/note/supplierId/supplierName/content）在 getMyScores（用 supplierId/verdict/category/requirementId/content/note）与 getReport（用 supplierId/supplierName/requirementId/category/content/note）一致；前端 `onToggleNote`/`renderReviewPanel` 参数名一致；noteId = `${supplierId}:${requirementId}` 前后端语义一致。✓
